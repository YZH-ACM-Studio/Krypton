# Krypton 领域上下文

本上下文统一 Krypton 题目创作、考试基础设施等领域中的产品术语，避免不同模块对同一概念使用不同含义。

## 考试基础设施

**考试活动（`ExamEvent`）**:
一次可独立管理终端、网络策略和座位的考试运行单元；可以不含 OJ 题目，也可以选择关联一场 Krypton Contest。
_Avoid_: 用空 Contest 代替外部考试、把 Contest 当成所有终端能力的根实体

**终端服务（`Krypton Endpoint Service`）**:
安装在考试机上的 Windows 自启动服务，持有稳定机器身份并通过受限模块执行网络策略、终端控制和状态上报；它不承载教师 WebUI 或学生考试页面。
_Avoid_: `KryptonVigilClient`、可远程执行任意命令的通用代理、动态 DLL 插件平台

**终端（`Endpoint`）**:
一台由稳定硬件 `machineId` 唯一派生 `endpointId`、并由 Endpoint Service 每次生成或恢复独立 CNG 凭据的考试机身份；`machineId` 决定身份归属，私钥证明当前连接。
_Avoid_: 在线 WebSocket、共享客户端 token、把 `machineId` 本身当作私钥证明、依赖整盘还原保留 endpointId

**终端机器登记（`EndpointRegistration`）**:
OJ 对完整硬件 fingerprint 与确定性 `endpointId` 的不可变一对一登记；Vigil 用它在本地身份丢失时恢复原 endpoint，或为首次出现的机器原子创建同一身份。旧 `EndpointEnrollmentBatch` 只保留既有终端的历史 ownership 兼容，不再是安装流程。
_Avoid_: 人工入网码、母盘完成命令、为同一 fingerprint 创建第二个 active endpoint

**网络策略模板（`PolicyTemplate`）**:
可被多场考试活动复用的网络访问规则草稿容器；系统级规则只表达域名、IP/CIDR 和端口，不表达 HTTPS 路径。
_Avoid_: URL 路径白名单、直接下发的无版本 JSON patch

**策略修订（`PolicyRevision`）**:
一次不可变的已发布网络策略版本；活动中的热更新通过发布并分配新修订完成，回滚通过重新分配历史修订完成。
_Avoid_: 原地覆盖当前策略、反向拼接回滚 patch

**策略执行会话（`EnforcementSession`）**:
一场考试活动在明确起止时间内对目标终端执行某个策略修订的实例；控制面短暂断线不会结束会话，硬结束时间或授权停止才会解除。
_Avoid_: 依赖学生 GUI 进程的短租约、没有硬结束时间的永久锁

**终端策略状态（`EndpointPolicyStatus`）**:
某台目标终端对一次执行会话的期望修订、实际应用修订和成功或失败结果，是 WebUI 判断策略是否真正生效的依据。
_Avoid_: 把服务端已发送当成终端已应用、只显示整批成功

**终端目标快照（`EndpointTargetSnapshot`）**:
策略启用时由教室、座位分配或用户组选择编译出的明确终端集合；范围变化只有发布新 revision 才能影响活动中的执行目标。
_Avoid_: 执行期间实时查询用户组、座位变化后静默增删终端

**终端监控策略（`EndpointMonitoringPolicy`）**:
一场考试活动用于检测 USB 存储和可疑进程并生成告警的规则；当前语义只包含观测与报告，不包含设备禁用或进程结束。
_Avoid_: U 盘封锁、进程拦截、把告警描述成已阻止

**预登录票据（`PreloginTicket`）**:
与一场考试活动、一个 OJ 用户和一台终端绑定的短期一次性启动凭据，用于拉起客户端并兑换 OJ 考试会话，不包含学生密码。
_Avoid_: 预存密码、可转移到其它机器的登录链接、长期访问 token

**终端就绪检查（`EndpointReadinessCheck`）**:
批量预登录前对学生资格、座位映射、终端在线、版本和能力进行的一次可审计预检；结果是诊断快照，不是长期授权令牌。
_Avoid_: 自动修复、把 USB/进程告警当作强制阻断、跳过确认直接启动

## 座位与学生身份

**教室布局快照（`RoomLayoutSnapshot`）**:
从码上坐数据库导出中显式导入并经校验确认的一版教室与座位布局，保留来源标识和 fingerprint；考试运行时不依赖码上坐实时数据。
_Avoid_: 实时跨库读取、两边同时编辑的布局、无来源版本的 JSON 副本

**实体座位（`PhysicalSeat`）**:
教室布局中由稳定 `seatId` 标识的座位；名称或编号 `label` 只用于展示，修改 label 不改变座位身份。
_Avoid_: 以 `seat_label` 为主键、把同名座位视为同一座位

**终端座位绑定（`EndpointSeatBinding`）**:
固定机房中一个已登记 `endpointId` 与一个实体座位的长期一对一关系，与具体考试和考生无关；整盘还原后仍由同一运行时 `machineId` 恢复该 endpoint，从而继续使用原绑定。
_Avoid_: 学生座位分配、每场考试重复绑定固定机器

**考试座位分配（`ExamSeatAssignment`）**:
一场考试活动中一个 OJ 学生身份与一个实体座位的分配关系；它引用 userbind 学生和用户组，不从码上坐创建另一套学生身份。
_Avoid_: 终端座位绑定、码上坐学生账号、按显示 label 关联

**座位分配草稿（`SeatAssignmentDraft`）**:
一场考试活动中尚未确认的学生座位方案；默认随机结果由持久化 seed 决定，刷新不会变化，人工锁定和换位优先于再次随机。
_Avoid_: 每次打开重新随机、无预览直接写入正式分配、复杂约束求解器

## 题集、课程与兑换权益

**题集（`ProblemSet`）**:
由 `TrainingDoc(docType:40)` 演进而来的可复用学习容器，包含阶段 DAG、题目引用和个人进度；存量无 `kind`/`kind:'training'` 文档按题集读取，新建文档写 `kind:'problem_set'`。
_Avoid_: 新建第二套集合迁移 training、复制题目、把课程和题集混在同一访问路由

**题集发现范围（`ProblemSetAudience`）**:
决定用户是否能在列表或可信入口发现题集的规则，可来自公开、userbind 用户组、课程引用或既有兑换权益；它不等同于阶段是否已经解锁。
_Avoid_: 只在前端隐藏、把能看见题集等同于能进入所有阶段、用 `enroll` 代替授权

**阶段访问权益（`ProblemSetStageEntitlement`）**:
允许一个用户访问题集中特定阶段的明确权益；授予某阶段时包含其传递先修阶段的访问权，但不伪造完成状态，真正进入仍需满足 DAG 进度。
_Avoid_: 自动标记先修完成、发出无法访问其前置内容的孤立阶段权益

**题集学习记录（`ProblemSetEnrollment`）**:
用户显式开始题集或成功兑换后创建的个人进度入口；它用于“我的题集”和进度统计，不是访问授权事实源，查看介绍页不会创建记录。
_Avoid_: GET 页面写报名状态、用报名状态控制直接 URL、撤销访问时删除提交进度

**兑换码批次（`RedemptionCodeBatch`）**:
由有权教师创建的一组同目标兑换码及其发行配置，记录权益目标、可兑换用户组、有效期、使用上限、创建者和审计；一个批次不承载任意权限表达式。
_Avoid_: 任意 JSON 权限包、跨多个无关目标的规则引擎、只有码值没有发行上下文

**兑换码（`RedemptionCode`）**:
可由系统生成或由创建者在创建时手工填写的兑换秘密；分为单次码和通用码，服务端只保存带版本的密钥 HMAC 查找摘要。手工码不设强度门槛，但仍须非空、唯一且可安全处理；创建后不改写码值。
_Avoid_: 明文长期存储、首次使用后更换码值、把“长期有效”等同于“多人共用”

**兑换权益授予（`RedemptionGrant`）**:
一次成功兑换向明确用户授予题集、题集阶段或课程访问的持久事实；停用原兑换码不自动撤回既有授予，撤销个人授予也不删除学习历史。
_Avoid_: 仅以兑换次数推断用户权益、停码时批量静默追回、离开兑换时用户组后自动失效

**课程题集引用（`CourseProblemSetReference`）**:
课程章节对一个题集整体或指定阶段的实时引用，为课程成员提供该范围的课程来源访问；题目成员仍以题集为事实源，移除引用不删除其它来源权益或学习进度。
_Avoid_: 把题集 `pids` 复制进课程、对课程引用做无需求的内容快照、信任用户伪造来源 query

**练习真实性策略（`PracticeIntegrityPolicy`）**:
由题集或课程使用上下文施加给学生 IDE/题面交互的规则，当前包含禁粘贴、仅允许 IDE 提交和复制时防 AI 注入；同一 Problem 在不同容器中可以有不同策略，Contest 不会因课程策略被全局限制。
_Avoid_: Problem 全局禁粘贴开关、只隐藏前端按钮、声称网页策略不可绕过

**可信练习上下文（`PracticeContext`）**:
服务端确认的用户、容器、阶段/章节、题目和策略 revision 组合，用于渲染受限 IDE、隔离草稿并标记提交来源；客户端 query 本身不是可信上下文。
_Avoid_: `?fromCourse=` 自报来源、跨题复用页面 token、把普通提交算作真实性上下文提交

**上下文完成记录（`ContextualCompletion`）**:
一次可信课程/题集上下文提交在评测成功后形成的容器进度事实；真实性容器只认适用上下文内的记录，普通全局 AC、其它课程和 Contest AC 不会冒充该完成。
_Avoid_: 继续只读全局 `ProblemStatus` 计算真实性进度、客户端声明完成目标、策略变化后重写历史结果

**真实性策略修订（`PracticeIntegrityRevision`）**:
课程或题集发布的不可变真实性策略版本；嵌套上下文按更严格规则合成并记录参与版本，新版本只约束新草稿/提交，不追溯撤销既有完成。
_Avoid_: 原地覆盖后重新解释历史提交、跨 revision 复用草稿、外层容器静默放宽内层策略

## 文件收集

**文件收集（`CollectRequest`）**:
一次由教师创建的独立收文件活动；老师圈定同一学校下的 userbind 用户组、定义槽位与截止时间并发布，学生按槽交文件，老师查看谁交了并打包下载。受众每次请求按当前组成员 live 计算；不打分、不退回、不进考试壳。
_Avoid_: 作业、任务、练习、Hydro homework、个人网盘 `/file`

**槽位（`CollectSlot`）**:
一次文件收集中老师定义的分类位置，含名称、必填或选填、允许扩展名和该槽文件数上限；学生按槽上传，必填槽都有当前文件才算交齐。已有人交过后不能再改槽位或类型。
_Avoid_: 无槽自由上传、把未标记文件自动归入默认槽、有人交过后再改槽位

**交文件**:
学生在截止前向所属文件收集的指定槽位上传或替换文件，并确认提交；以服务器确认时刻为准，`now >= dueAt` 后不能再传或替换。一人一份，没有队长代交。
_Avoid_: 当作作业提交、写入 Record/testdata、免登录链接、考试机收文件

**收集提交（`CollectSubmission`）**:
每个用户对一次文件收集恰好一行的确认记录；`submitted` 表示已交齐。截止前可换当前文件，历史版本保留。退组后提交与文件不删，只从待交名单消失，打包仍带已交文件。
_Avoid_: Record、ContestStatus、作业成绩表

**文件名模板（`fileNameTemplate`）**:
一次收集一条模板，与 `packLayout`（`nested|flat`）一起存在请求上；默认 `{originalName}` 与 `nested`。缺字段按默认读取，禁止请求路径回填。首份提交后与槽位一同锁定。
_Avoid_: 请求路径回填、有人提交后再改模板或 layout、班级目录

**分配文件名（`assignedName`）**:
展示、打包、下载时由模板与 live userbind 派生的名字，不入库；blob 路径保持 `collect/{domain}/{request}/{uid}/{fileId}`。未交 CSV 按每个必填槽给预期名；已交 CSV 含 assigned、original 与 sha256。当前文件 sha256 重复只是教师进度提示。
_Avoid_: 持久化改名、改写 blob 路径、用哈希去重拒绝上传或当作抄袭裁定

## 虚拟参赛

**虚拟参赛尝试（`VirtualContestAttempt`）**:
一个真实用户针对一场已结束源比赛启动的一次独立计时成绩上下文，保存相对开始/结束、规则快照和虚拟统计；它不复用源比赛 ContestStatus，也不是 Hydro 虚拟用户。
_Avoid_: 把赛后补题改名为 VP、在正式榜单插入虚拟行、允许反复刷多次最佳成绩

**虚拟参赛记录（`VirtualContestRecord`）**:
关联 `virtualAttemptId` 与 `sourceContestId` 的普通评测 Record；它更新个人全局做题事实和独立 VP 成绩，但不进入源比赛正式计分、Rating、RP、气球或真实性课程进度。
_Avoid_: 只凭 `tid` 猜 VP、写入源 ContestStatus、把普通补题记录导入 attempt

**虚拟参赛榜单（`VirtualContestScoreboard`）**:
只由 VP attempt 的相对时间与独立状态计算的榜单；活跃用户只能看自己的行，完成后才与其它虚拟成绩比较，并始终与原正式榜单分离。
_Avoid_: 使用墙钟开始时间比较不同 attempt、进行中泄漏其它人题目状态、将 VP 名次称为正式名次

**Vigil 源比赛的虚拟参赛**:
正式比赛曾配置 Vigil / `client_required`，但赛后 VP 仍是普通网页自律训练；它只复用题目、赛制、时长和封榜语义，不继承 Client、网络锁、监控采集、座位或浏览器封锁。
_Avoid_: 把“源比赛使用过 Vigil”误解成 VP 也要启动 Client、创建 `VigilVirtualSession`、恢复正式比赛策略或宣传为强防作弊

**防 AI 标记（`AntiAiMarker`）**:
题面中由作者定位、关联任意隐藏文本的结构化内联标记；视觉渲染不显示，只有复制选区跨过标记且当前上下文启用注入时才进入剪贴板。
_Avoid_: 在可见 Markdown 中混入大量零宽 Unicode、污染代码编辑器/打印/PDF、无位置地给每次复制追加同一段文字

## 单题题库

**结构化题**:
具有明确题型和专用配置的一道独立 Problem，包括单选、多选、判断、填空、主观、程序填空和代码实现题；代码实现题内部仍使用 `problemKind:'function'`。
_Avoid_: 一张卷子中的小题、泛称客观题

**知识导图（`KnowledgeMap`）**:
可独立隐藏或公开、由唯一根节点构成的一棵知识树；不同导图允许存在同名节点和相同普通标签，节点身份必须同时包含所属导图。
_Avoid_: `_id:'global'` 单例导图、要求全站节点名或标签唯一

**知识标签**:
从知识导图节点选择并包含其带标签祖先的题目分类；结构化题的知识标签不能自由输入。
_Avoid_: 自由标签、逗号分隔标签

**题目导图归属与知识节点引用（`knowledgeMapId + knowledgeNodeIds`）**:
题目知识分类的联合事实源；一题只能引用 `knowledgeMapId` 所属导图中的一个或多个节点，服务端据此重新物化 `pdoc.tag` 供搜索，客户端不能跨图混选或直接写派生标签。
_Avoid_: 只凭 `knowledgeNodeIds` 猜所属导图、跨图节点混选、把 `pdoc.tag` 当成第二个可编辑事实源

**课程导图绑定（`TrainingDoc.mindmapId`）**:
课程对一张公开知识导图的可选引用；只用于课程知识视图，不复制节点，也不限制章节可引用的题目。课程视图只高亮本课程内、当前用户可见且直接选择该图节点的题目。
_Avoid_: 让课程拥有导图、自动裁剪导图副本、阻止跨图或无节点题进入课程

## 程序填空

**程序填空题（`program_fill`）**:
一道给定代码上下文并包含任意多个填空区的单题；每个填空区只允许一行作答，出题时固定采用文本评测或编译评测。
_Avoid_: 只有一个 `main` 空的程序填空、把多个空拆成多道题

**填空区**:
程序填空题中一个带稳定标识的单行作答位置；同一题可以包含任意多个填空区。
_Avoid_: `main` region、自由命名且未校验的 region

**文本评测**:
各填空区按相同权重独立比对并累计得分，不提供逐空自定义权重；比对规则为统一换行并去除首尾空白，大小写敏感。
_Avoid_: 整题全对才得分、逐空自定义分值

**编译评测**:
把所有填空区的作答拼回完整程序，再按题目的测试点整体评分，不为单个填空区单独计分。
_Avoid_: 按填空区分别编译、按填空区单独给分

## 代码评测题创作

**代码评测草稿**:
已经取得真实题目编号、但评测模板或测试数据尚未完成的隐藏题目；它可以继续编辑，但不可发布或接受作答。
_Avoid_: 可评测的不完整题目、创建前临时题目

**代码评测就绪状态（`codeEvaluationStatus`）**:
代码实现题和编译评测程序填空题的服务端状态，只有 `ready` 才能发布、被容器引用或提交；`draft` 必须保持隐藏并在所有使用入口 fail closed。
_Avoid_: 仅靠前端禁用发布、配置不完整但仍可评测

**测试点映射**:
一个测试点对已实际上传的输入文件与输出文件的配对关系，只能引用题目当前存在的测试数据文件。
_Avoid_: 手填文件名、引用尚未上传的文件

**可填写区**:
老师在唯一完整源码中标注、由系统赋予稳定标识的连续完整行范围；同一模板中的作答区必须非空，并与公开区彼此互斥、不可重叠或嵌套。
_Avoid_: 手写 marker、自由填写 region ID、未校验的代码范围

**区域行坐标**:
源码统一为 LF 后采用 0-based、起点包含终点不包含的完整行区间；代码实现区可含多行，程序填空区必须恰好一行。作答顺序只按 `(startLine,endLine,id)` 从源码位置派生，不存在独立拖拽顺序。
_Avoid_: 列坐标、marker 坐标、靠附近文本猜测失效区域

**函数区**:
内部 `problemKind:'function'` 题中允许学生编写任意连续多行代码的作答区；可用于函数体、完整函数、类定义或其它指定代码块，一题可有一个或多个区域。
_Avoid_: 假定只能填写函数体、把多个作答区拆成多道题

**函数公开接口**:
代码实现题作答区只提供稳定区域标识、可选标题与可选说明；标题为空时按源码顺序显示“作答区 N”，不要求函数签名。
_Avoid_: 必填函数签名、独立 region order、把私有 main/driver/helper 当作学生起始代码

**公开代码骨架**:
作者在唯一完整源码中显式标记的公开完整行，与原位作答区共同组成学生连续代码表面；未标记行默认私有。学生 DTO 只含按源码顺序排列的公开 code block 与 region descriptor。
_Avoid_: 自动公开所有非答案行、完整评测模板、标准答案、源码坐标或隐藏行数量

**私有评测模板**:
仅服务端和有权出题者可见、用于拼接与编译的完整代码；代码实现题的测试驱动、隐藏辅助代码和程序填空标准内容都属于该模板。
_Avoid_: 学生起始代码、公开代码骨架
